const placeHolder = 'border-0 rounded-lg m-0 p-0 aspect-square ';
// w-x
const large = placeHolder + 'w-20';
const base = placeHolder + 'w-10';
const small = placeHolder + 'w-5';

export const styles = {
  large,
  base,
  small,
};
